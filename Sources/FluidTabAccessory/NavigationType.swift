import Foundation

/// This enum aims to define different navigation types within the FluidTabAccessory framework.
/// Currently there are two types of navigation supported.
enum NavigationType {
    case sheetFullCover
    case tabNavigation
}
